#!/usr/bin/env bash

set -euo pipefail

list_all_versions() {
	git ls-remote --tags --refs https://gitea.com/gitea/tea.git | grep -o 'refs/tags/.*' | cut -d/ -f3- | sed 's/^v//' | sort
}

list_all_versions
